---
name: Bug report
about: Something concrete is broken — a diagram, a build script, a rendering issue
title: "[BUG] "
labels: bug
---

**Use this template for concrete, reproducible problems** — a diagram that doesn't render, a `tools/` script that fails to run, text that overlaps or overflows in a PDF/SVG. For a disagreement with the model itself (a schema, a naming choice, a missing case), use [Discussions](../../discussions) instead — see `CONTRIBUTING.md` for why.

## What's broken

<!-- One or two sentences. Which file, which script, which diagram. -->

## Steps to reproduce

<!-- e.g. "Run tools/diagrams/build_safety.py from a clean diagrams/ folder" -->

1.
2.
3.

## Expected vs. actual

**Expected:**

**Actual:**

## Environment

- OS:
- Python version (if running a `tools/` script):
- Where you saw this (GitHub web, local build, the published PDF, lunex.cloud):

## Screenshots / output

<!-- Paste any error output or attach a screenshot — this is usually the fastest way to get a fix. -->
